server:
  port: 9003

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        #缓存nginx的1111端口，做集群
        #server-addr: 192.168.110.100:1111

management:
  endpoints:
    web:
      exposure:
        include: "*"